#include <stdio.h>

int main()
{
	// This is a comment blah!!
	/* Man this is so good
	I think C is very coool
	Even though it can be really painfull */	
	
	/* escape sequence = character combination consisting of a backslash /
						 followed by a letter or combination of digits.
						 They specify action within a line or string of text. 
						 /t = tab
						 /n = newline
						 Also use when you want to add quotes within your code, so the compiler
						 doesn't think its the end of your string.
	*/
	
	
	printf("\"I like pizza\" - Abraham Lincoln quote haha :)");
	printf("\'I like pizza\' - Abraham Lincoln quote haha :)");

}
